pytest>=8.0
pytest-rerunfailures>=14.0
pytest-json-report>=1.5   # для статистики прогона (см. .github/workflows)
requests>=2.31
allure-pytest>=2.13

# pytest-xdist (-n auto) сознательно не используем: каждый воркер поднимает
# СВОЙ локальный сервер «Пульта» отдельным процессом — под параллельной
# нагрузкой ловили ConnectionError даже со сдвигом портов по воркеру.
# Набор маленький (23 теста, ~1.5с) — параллелить незачем, а стабильность
# важнее полутора секунд.
